{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "composer install",
            "type": "shell",
            "command": "docker-compose exec -T -w  /var/www/html webserver composer install"
        }
    ]
}